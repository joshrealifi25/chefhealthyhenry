# Code ownership for chefhealthyhenry.com
#
# Review here is ADVISORY, not required. Nothing in this file blocks a merge.
# Listing a path auto-requests @joshrealifi25's review so he sees the change,
# and any collaborator can still merge without waiting for it.
#
# What DOES block a merge is CI: the "Lint, types, content, build" check must
# pass. Run `npm run check` before pushing and it will pass.
#
# These are the files where a mistake breaks checkout, loses sales, or takes
# the site down, rather than just looking wrong. Worth a second pair of eyes
# even though nothing forces it.

# Commerce + automated PDF delivery (a mistake here = broken checkout / lost sales)
/src/app/api/            @joshrealifi25
/src/lib/fulfillment.ts  @joshrealifi25
/src/data/cookbooks.ts   @joshrealifi25

# Site config: redirects, canonical URL, dependencies, CI/ownership
/next.config.ts          @joshrealifi25
/src/lib/site.ts         @joshrealifi25
/package.json            @joshrealifi25
/package-lock.json       @joshrealifi25
/.github/                @joshrealifi25
