@AGENTS.md

# Chef Healthy Henry

All project instructions live in `AGENTS.md`, imported above. It is the single
source of truth, so Claude Code, Cursor, and cloud agents all follow the same
rules. Add new rules there rather than here, then run
`bash scripts/sync-agent-rules.sh`.
